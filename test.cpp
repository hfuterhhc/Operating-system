#include"test.h"

test::test(){
}

void test::config(int *N1,int *N2,int *T1,int *T2){

    *N1=10;
    *N2=10;
    *T1=1;
    *T2=1;

    /*
       FILE *fp;
       if (!(fp=fopen("../test.txt","r")))
        {
           cout<<"读取失败"<<endl;
           exit(1);
         }
        fscanf(fp,"%d %d %d %d",N1,N2,T1,T2);
        fclose(fp);
     */
}


